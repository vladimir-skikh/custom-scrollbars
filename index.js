new SimpleBar(
  document.getElementById('simplebar-example'),
  { autoHide: false }
);

const osInstance = OverlayScrollbarsGlobal.OverlayScrollbars(document.getElementById('overlayscrollbars-example'), {});
